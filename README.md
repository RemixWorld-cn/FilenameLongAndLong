# FilenameLongAndLong
## 这可能是世界上最长的文件扩展名
### 扩展名为:.
## 支持的文件类型
=== 图像类 ===
SVG数据：<svg><circle cx="50" cy="50" r="40"/></svg>
PNG数据：[PNG文件头 89 50 4E 47]
JPEG数据：[JFIF文件头 FF D8 FF]
GIF数据：[GIF89a文件头]
ICO数据：[图标文件头]
BMP数据：[BM文件头]
TIFF数据：[II*或MM*文件头]
WebP数据：[RIFF文件头]
AVIF数据：[ftypavif]
RAW数据：[相机原始数据]
PSD数据：[8BPS文件头]
AI数据：[Adobe Illustrator]
EPS数据：[%!PS-Adobe]
INDD数据：[Adobe InDesign]
XCF数据：[gimp xcf]
TGA数据：[Truevision TGA]
DNG数据：[数字负片]
HEIC数据：[heic文件头]

=== 视频类 ===
MP4数据：[ftypmp4]
AVI数据：[RIFFAVI]
MOV数据：[mov文件头]
WMV数据：[ASF文件头]
FLV数据：[FLV文件头]
MKV数据：[EBML文件头]
WEBM数据：[EBMLwebm]
MPEG数据：[MPEG文件头]
3GP数据：[ftyp3gp]
M4V数据：[M4V文件头]
RMVB数据：[RealMedia]
VOB数据：[DVD视频]
OGV数据：[Ogg视频]
TS数据：[MPEG-TS流]

=== 音频类 ===
MP3数据：[ID3标签或FF FB]
WAV数据：[RIFFWAVE]
AAC数据：[ADTS文件头]
FLAC数据：[fLaC文件头]
OGG数据：[OggS文件头]
WMA数据：[ASF音频]
M4A数据：[M4A文件头]
AIFF数据：[FORM AIFF]
AMR数据：[AMR文件头]
MIDI数据：[MThd文件头]
OPUS数据：[Opus文件头]

=== 压缩包类 ===
ZIP数据：[PK文件头 50 4B 03 04]
7z数据：[7z文件头 37 7A BC AF 27 1C]
RAR数据：[RAR文件头 52 61 72 21]
TAR数据：[ustar文件头]
GZ数据：[1F 8B]
BZ2数据：[BZh文件头]
XZ数据：[FD 37 7A 58 5A 00]
CAB数据：[MSCF文件头]
ISO数据：[ISO 9660]
DMG数据：[Apple Disk Image]
IMG数据：[磁盘映像]
WIM数据：[MSWIM文件头]
APK数据：[Android APK/ZIP]
DEB数据：[Debian包]
RPM数据：[RPM文件头]

=== 文档类 ===
PDF数据：[%PDF-文件头]
DOC数据：[OLE2复合文档]
DOCX数据：[PK文件头 + docx]
PPT数据：[OLE2复合文档]
PPTX数据：[PK文件头 + pptx]
XLS数据：[OLE2复合文档]
XLSX数据：[PK文件头 + xlsx]
ODT数据：[PK文件头 + odt]
ODS数据：[PK文件头 + ods]
ODP数据：[PK文件头 + odp]
RTF数据：[{\rtf]
TXT数据：[纯文本字符]
MD数据：[Markdown文本]
CSV数据：[逗号分隔文本]
JSON数据：[{"key":"value"}]
YAML数据：[key: value]
TOML数据：[key = "value"]

=== 编程语言类 ===
C文件：[#include <stdio.h>]
C++文件：[#include <iostream>]
C#文件：[using System;]
Java文件：[public class Main {]
Python文件：[#!/usr/bin/env python3]
JavaScript文件：[#!/usr/bin/env node]
TypeScript文件：[interface Props {]
PHP文件：[<?php]
Ruby文件：[#!/usr/bin/env ruby]
Go文件：[package main]
Rust文件：[fn main() {]
Swift文件：[import Swift]
Kotlin文件：[fun main() {]
Scala文件：[object Main {]
Perl文件：[#!/usr/bin/perl]
Lua文件：[#!/usr/bin/lua]
Dart文件：[void main() {]
Julia文件：[#!/usr/bin/julia]
Haskell文件：[module Main where]
Elixir文件：[defmodule Main do]
Shell文件：[#!/bin/bash]
Batch文件：[@echo off]
SQL文件：[CREATE TABLE]

=== 3D/模型类 ===
STL数据：[solid 3D模型]
OBJ数据：[3D对象文件]
FBX数据：[FBX文件头]
GLTF数据：[glTF JSON]
GLB数据：[glTF二进制]
PLY数据：[ply文件头]
PCD数据：[PCD点云]

=== 其他 ===
Blend数据：[BLEND文件头]
Maya数据：[Maya场景]
3ds Max数据：[MAX文件头]
Cinema 4D数据：[C4D文件头]
Unity数据：[Unity资产]
Unreal数据：[Unreal资产]
Godot数据：[Godot场景]
Git数据：[Git仓库]
MSI数据：[Windows安装包]
APP数据：[macOS应用包]
PKG数据：[macOS安装包]
TTF数据：[TrueType字体]
OTF数据：[OpenType字体]
WOFF数据：[Web开放字体]
EXE数据：[MZ文件头 4D 5A]
VMDK数据：[VMware磁盘]
VDI数据：[VirtualBox磁盘]
VHD数据：[虚拟硬盘]
PEM数据：[-----BEGIN CERTIFICATE-----]
CRT数据：[证书文件]
KEY数据：[私钥文件]
TORRENT数据：[BitTorrent文件]
SAV数据：[游戏存档]
ROM数据：[游戏ROM]
NES数据：[NES游戏]
DAT数据：[数据文件]
BAK数据：[备份文件]
LOG数据：[日志文件]
XML数据：[<?xml version="1.0"?>]
HTML数据：[<!DOCTYPE html>]

## 全编译器

### 一、 图像类 (Image Formats)

| 目标后缀 | 主流/官方核心库 | 官方下载与规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.svg** | `librsvg` | [gitlab.gnome.org/GNOME/librsvg](https://gitlab.gnome.org/GNOME/librsvg) | SVG 2.0 | 解析 XML 头部，提取 `<svg>` 标签的 `viewBox` 和 `xmlns` 属性。 |
| **.png** | `libpng` | [libpng.org](http://www.libpng.org/pub/png/libpng.html) | PNG 3.0 (HDR, APNG) | 寻找 `IHDR` 块提取宽高色深，读取 `IDAT` 并使用内置 zlib 解压，验证 `eXIf` 块。 |
| **.jpg/.jpeg** | `libjpeg-turbo` | [libjpeg-turbo.org](https://libjpeg-turbo.org/) | JPEG XL / JPEG 2000 | 寻找 `FF D8` (SOI)。跳过图像解码，直接读取 `SOF0/SOF2` 提取最新色彩空间标识。 |
| **.ico** | Microsoft 规范 | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | Windows 图标格式 | 读取 `ICONDIR` 头部，提取图像数量及第一个图标的尺寸和色彩深度。 |
| **.bmp** | Microsoft 规范 | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | BMP / DIB | 验证 `42 4D` (BM) 幻数，读取 `BITMAPINFOHEADER` 提取位深和压缩类型。 |
| **.tif/.tiff** | Adobe TIFF SDK | [adobe.com/support](https://www.adobe.com/support) | TIFF 6.0 / BigTIFF | 验证 `II*` 或 `MM*` 幻数，解析 IFD (Image File Directory) 链表提取首个图像偏移量。 |
| **.webp** | `libwebp` (Google) | [developers.google.com/speed/webp](https://developers.google.com/speed/webp/download) | WebP 2, 动图 | 解析 `RIFF` 头，遍历 `VP8 `, `VP8L` 或 `VP8X` 块，提取 Alpha 通道和动画元数据。 |
| **.avif** | `libavif` | [github.com/AOMediaCodec/libavif](https://github.com/AOMediaCodec/libavif) | AV1 Image Format | 解析 `ftypavif` 盒，读取 `meta` 盒中的 `iloc` 和 `ispe` 获取 AV1 编码的图像分辨率。 |
| **.raw/.dng** | `dcraw` / Adobe DNG | [adobe.com/support/downloads](https://www.adobe.com/support/downloads) | DNG 1.6+ | 基于 TIFF 结构解析，提取相机原始数据的黑白平衡矩阵和 CFA 布局。 |
| **.psd/.psb** | Adobe PSD 规范 | [adobe.com/devnet](https://www.adobe.com/devnet) | Photoshop 2026 特性 | 验证 `8BPS` 幻数，读取版本字段（1 为 PSD，2 为 PSB），提取图层数量。 |
| **.ai/.eps** | Adobe Illustrator | [adobe.com/products/illustrator](https://www.adobe.com/products/illustrator) | EPS 3.0 / PDF 封装 | 验证 `%!PS-Adobe` 头部，解析 DSC (Document Structuring Conventions) 注释提取边界框。 |
| **.indd** | Adobe InDesign | [adobe.com/products/indesign](https://www.adobe.com/products/indesign) | InDesign 2026 | 读取专有数据库头部，验证文档的页面尺寸和主版式（Master Pages）索引。 |
| **.xcf** | GIMP | [gimp.org](https://www.gimp.org/) | XCF v14+ | 验证 `gimp xcf` 幻数，解析属性列表提取画布尺寸和图层混合模式。 |
| **.tga** | Truevision TGA | [en.wikipedia.org/wiki/Truevision_TGA](https://en.wikipedia.org/wiki/Truevision_TGA) | TGA 2.0 | 读取 18 字节 Header，提取像素深度和图像描述符（Alpha 通道位数）。 |
| **.heic** | `libheif` | [github.com/strukturag/libheif](https://github.com/strukturag/libheif) | HEIF / HEIC | 解析 `ftypheic` 盒，读取 `meta` 盒中的 `iinf` 和 `iprp` 提取 HEVC 编码属性。 |

### 二、 视频类 (Video Formats)

| 目标后缀 | 主流/官方核心库 | 官方下载与规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.mp4/.m4v/.3gp** | `FFmpeg`, `mp4box` | [ffmpeg.org](https://ffmpeg.org/download.html) | **H.266 (VVC)**, AV1, V-PCC | **Box/Atom 递归遍历**。寻找 `moov -> trak -> mdia -> minf -> stbl -> stsd`，读取最新 Codec 标识符（如 `av01`, `vvc1`）。 |
| **.avi** | Microsoft RIFF | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | RIFF AVI | 验证 `RIFF` 和 `AVI ` 幻数，解析 `hdrl -> avih` 块提取视频帧率和流数量。 |
| **.mov** | Apple QuickTime | [developer.apple.com/quicktime](https://developer.apple.com/quicktime/) | QuickTime VR / ProRes | 与 MP4 共享 Box 结构。重点解析 `clef` 或 `prof` 原子提取专业级色彩空间信息。 |
| **.wmv** | Microsoft ASF | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | Windows Media Video 9 | 解析 ASF 对象结构，读取 File Object 获取总时长，Stream Object 提取 WMV3 编码标识。 |
| **.flv** | Adobe Flash Video | [adobe.com/devnet](https://www.adobe.com/devnet) | RTMP / FLV | 验证 `FLV` 幻数，读取 9 字节 Header 和首个 Tag 的类型（Audio/Video/Script）。 |
| **.mkv/.webm** | `libmatroska` | [matroska.org](https://matroska.org/downloads/) | AV1 in Matroska | 解析 EBML 树状结构。寻找 `Segment -> Tracks -> TrackEntry` 提取最新 Codec ID。 |
| **.mpg/.mpeg** | MPEG 组织 | [mpeg.org](https://mpeg.org/) | MPEG-1 / MPEG-2 | 寻找 `00 00 01 B3` (Sequence Header) 提取视频分辨率和帧率。 |
| **.rmvb** | RealNetworks | [real.com](https://www.real.com/) | RealMedia VB | 验证 `.RMF` 幻数，解析 PROP (Properties) 块提取最大比特率。 |
| **.vob** | DVD Forum | [dvdforum.org](https://www.dvdforum.org/) | DVD-Video | 基于 MPEG-2 程序流。寻找 `00 00 01 BA` (Pack Header)，解析 PES 包提取音频流 ID。 |
| **.ogv** | Xiph.Org | [xiph.org/ogg](https://xiph.org/ogg/) | Ogg Theora | 解析 Ogg 页结构，识别 Theora 视频流和 Vorbis 音频流的头部。 |
| **.ts** | DVB / MPEG | [dvb.org](https://dvb.org/) | MPEG-TS 5G 广播流 | 寻找 `0x47` 同步字节，解析 PAT/PMT 表获取 PID 映射。 |

### 三、 音频类 (Audio Formats)

| 目标后缀 | 主流/官方核心库 | 官方下载与规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.mp3** | `libmpg123` | [mpg123.de](https://mpg123.de/) | MPEG-1/2 Audio Layer III | 寻找 `FF FB` 或 `ID3` 标签。解析 Frame Header 提取比特率、采样率和声道模式。 |
| **.wav** | Microsoft RIFF | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | WAVEFORMATEXTENSIBLE | 验证 `RIFF` 和 `WAVE`。解析 `fmt ` 块提取音频格式代码（如 PCM, IEEE Float）。 |
| **.aac** | `libfaac` / FFmpeg | [ffmpeg.org](https://ffmpeg.org/download.html) | AAC-LC / HE-AAC v2 | 解析 ADTS 头（12位同步字 `0xFFF`），提取 Profile（如 LC 或 HE）和采样率索引。 |
| **.flac** | Xiph.Org | [xiph.org/flac](https://xiph.org/flac/) | FLAC 1.4+ | 验证 `fLaC` 幻数。解析 METADATA_BLOCK (STREAMINFO) 提取采样率、位深和总样本数。 |
| **.ogg** | Xiph.Org | [xiph.org/ogg](https://xiph.org/ogg/) | Ogg Vorbis / Opus | 解析 `OggS` 页头。识别 Vorbis 评论或 Opus 头部信息。 |
| **.wma** | Microsoft ASF | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | Windows Media Audio 9 | 解析 ASF 对象结构，读取 Stream Properties Object 提取 WMA 编码器的配置参数。 |
| **.m4a** | Apple / MPEG | [developer.apple.com](https://developer.apple.com/) | ALAC / AAC | 基于 MP4 容器。定位 `moov -> trak -> mdia -> minf -> stbl -> stsd`，读取 `mp4a` 或 `alac` 盒。 |
| **.aif/.aiff** | Apple | [developer.apple.com](https://developer.apple.com/) | AIFF-C | 验证 `FORM` 和 `AIFF`。解析 `COMM` 块提取压缩类型和采样率。 |
| **.amr** | 3GPP | [3gpp.org](https://www.3gpp.org/) | AMR-NB / AMR-WB | 验证 `#!AMR` 幻数。读取帧头提取编码模式（4.75 kbit/s 到 23.85 kbit/s）。 |
| **.mid/.midi** | MIDI.org | [midi.org](https://www.midi.org/) | General MIDI 2 | 验证 `MThd` 幻数。解析 Header Chunk 提取格式类型、轨道数和时基（Ticks per Quarter Note）。 |
| **.opus** | Xiph.Org / IETF | [opus-codec.org](https://opus-codec.org/) | RFC 6716 | 解析 `OpusHead` 幻数。提取版本、通道数、预跳过（Pre-skip）和输出增益。 |

### 四、 压缩包与磁盘映像类 (Archive & Disk Image)

| 目标后缀 | 主流/官方核心库 | 官方下载与规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.zip/.docx/.xlsx/.apk** | `zlib`, PKWARE | [pkware.com/support/zip-app-note](https://www.pkware.com/support/zip-app-note) | **ZIP64 扩展**, OOXML | 从文件末尾向前扫描 `50 4B 05 06` (EOCD)。提取 Central Directory 偏移量，直接跳转读取，支持 8字节 ZIP64 长度。 |
| **.tar** | GNU `tar` | [gnu.org/software/tar](https://www.gnu.org/software/tar/) | PAX 扩展头 | 读取连续 512 字节 Block。解析 `ustar` 魔数，处理超过 8GB 文件的 PAX 扩展头。 |
| **.gz** | `zlib` | [zlib.net](https://zlib.net/) | RFC 1952 | 验证 `1F 8B` 幻数。读取 CM (Compression Method) 和 FLG 字段，解压 Deflate 流。 |
| **.bz2** | `bzip2` | [sourceware.org/bzip2](https://sourceware.org/bzip2/) | bzip2 1.0.8 | 验证 `BZh` 幻数。读取块大小（1-9），解析 Huffman 编码的 Block 头。 |
| **.xz** | Tukaani | [tukaani.org/xz](https://tukaani.org/xz/) | XZ Utils 5.6+ | 验证 `FD 37 7A 58 5A 00` 幻数。解析 Stream Header 和 Block Header 提取 LZMA2 参数。 |
| **.cab** | Microsoft | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | Microsoft Cabinet | 验证 `MSCF` 幻数。解析 Header 提取文件夹数量和文件偏移量。 |
| **.iso** | ISO 9660 | [iso.org](https://www.iso.org/) | ISO 9660 / Joliet / UDF | 寻找第 16 个 2048 字节扇区。读取 Volume Descriptor 提取卷标和文件系统类型。 |
| **.dmg** | Apple | [developer.apple.com](https://developer.apple.com/) | Apple Disk Image | 验证 `koly` 尾部块。解析 UDIF 资源分叉提取压缩编码格式（如 UDBZ, UDZO）。 |
| **.img** | 通用磁盘映像 | [en.wikipedia.org/wiki/IMG_(file_format)](https://en.wikipedia.org/wiki/IMG_(file_format)) | Raw Disk Image | 读取首个扇区，识别 MBR/GPT 分区表或文件系统超级块（如 FAT32, ext4）。 |
| **.wim** | Microsoft | [learn.microsoft.com/en-us/windows-hardware/](https://learn.microsoft.com/en-us/windows-hardware/) | Windows Imaging Format | 验证 `MSWIM` 幻数。解析 WIM Header 提取映像数量和压缩算法（LZX 或 XPRESS）。 |
| **.deb** | Debian | [debian.org/doc/debian-policy](https://www.debian.org/doc/debian-policy/) | Debian Package | 验证 `!<arch>` 幻数。依次读取 `debian-binary`, `control.tar.gz`, `data.tar.gz` 成员。 |
| **.rpm** | RPM.org | [rpm.org](https://rpm.org/) | RPM Package Manager | 验证 `ED AB EE DB` 幻数。解析 Lead, Signature Header 和 Main Header 提取包名和架构。 |
| **.rar** | RARLAB | [rarlab.com](https://www.rarlab.com/) | RAR7 | 验证 `Rar!\x1a\x07` 签名，解析 Archive Header 和 File Header 提取压缩算法。 |
| **.7z/.x7z** | 7-Zip | [7-zip.org](https://www.7-zip.org/) | 7z 格式 | 验证 `7z\xbc\xaf\x27\x1c` 签名，解析 SignatureHeader 和编码流。 |

### 五、 文档与数据交换类 (Document & Data Exchange)

| 目标后缀 | 主流/官方核心库 | 官方下载与规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.pdf** | Adobe / ISO | [adobe.com/go/pdf](https://www.adobe.com/go/pdf) (ISO 32000) | **PDF 2.0** (AES-256) | 寻找 `%PDF-` 头部。从尾部寻找 `startxref`，跳转到 `xref` 交叉引用表，验证 `Encrypt` 字典。 |
| **.doc/.xls/.ppt** | OLE2 规范 | [microsoft.com/openspecs](https://learn.microsoft.com/en-us/openspecs/) | OLE 复合二进制 | 验证 `D0 CF 11 E0 A1 B1 1A E1` 幻数。解析 FAT 链表，寻找 `Root Entry` 目录流。 |
| **.docx/.xlsx/.pptx** | OOXML | [ecma-international.org](https://www.ecma-international.org/) | OOXML (Office 2026) | 基于 ZIP。定位 `[Content_Types].xml`，解析关系文件提取文档主体 XML 部分。 |
| **.odt/.ods/.odp** | ODF | [oasis-open.org](https://www.oasis-open.org/) | ODF 1.4 | 基于 ZIP。读取 `mimetype` 文件，解析 `content.xml` 提取文本和表格结构。 |
| **.rtf** | Microsoft | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | RTF 1.9 | 验证 `{\rtf1` 头部。解析控制字（Control Words）提取字符编码和字体表。 |
| **.txt** | ASCII / UTF-8 | [iana.org/assignments/character-sets](https://www.iana.org/assignments/character-sets) | UTF-8 | 读取文件流，验证 BOM (Byte Order Mark)，统计字符数和换行符格式（CRLF/LF）。 |
| **.md** | CommonMark | [commonmark.org](https://commonmark.org/) | GFM (GitHub Flavored) | 解析块级元素（标题、列表、代码块），验证 GFM 表格和任务列表语法。 |
| **.csv** | IETF | [datatracker.ietf.org/doc/html/rfc4180](https://datatracker.ietf.org/doc/html/rfc4180) | RFC 4180 | 状态机解析器。处理引号转义和逗号分隔，验证表头与数据行的列数一致性。 |
| **.json** | ECMA | [ecma-international.org](https://www.ecma-international.org/) | ECMA-404 | 递归下降解析器。验证对象、数组、字符串、数字和布尔值的嵌套结构，检查尾随逗号。 |
| **.yml/.yaml** | YAML.org | [yaml.org](https://yaml.org/) | YAML 1.2 | 基于缩进的解析器。验证锚点（`&`）、别名（`*`）和多重文档分隔符（`---`）。 |
| **.toml** | TOML.io | [toml.io](https://toml.io/) | TOML 1.0.0 | 解析键值对、表（Tables）和数组。验证日期时间格式和浮点数精度。 |

### 六、 编程语言与脚本类 (Programming Languages & Scripts)

| 目标后缀 | 官方编译器/解释器 | 官方下载与 Release 主页 | 最新语言标准 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.c/.h** | GCC, Clang | [gcc.gnu.org](https://gcc.gnu.org/releases.html) / [llvm.org](https://llvm.org/) | **C23** | 词法分析器。扫描最新关键字（如 `typeof`, `constexpr`），验证预处理器指令（`#include`, `#define`）。 |
| **.cpp/.cc/.cxx** | GCC, Clang | [gcc.gnu.org](https://gcc.gnu.org/releases.html) / [llvm.org](https://llvm.org/) | **C++26** | 词法分析器。扫描最新关键字（如 `co_await`, `requires`, 反射特性），验证模板嵌套。 |
| **.cs** | Roslyn | [github.com/dotnet/roslyn](https://github.com/dotnet/roslyn) | C# 12/13 | 扫描类、结构体和记录（`record`）声明。验证最新的模式匹配和异步流（`IAsyncEnumerable`）语法。 |
| **.pr** | Prolog | [swi-prolog.org](https://www.swi-prolog.org/) | ISO Prolog | 解析子句和规则。验证事实（Facts）和查询（Queries）的语法结构。 |
| **.rb** | Ruby | [ruby-lang.org](https://www.ruby-lang.org/) | Ruby 3.3+ | 扫描类、模块和方法定义。验证块（Blocks）和 Lambda 语法。 |
| **.swift** | Apple Swift | [swift.org](https://www.swift.org/) | Swift 6.0 | 扫描 `func`, `struct`, `class`。验证最新的并发特性（`async/await`, Actors）语法。 |
| **.kt/.kts** | Kotlin | [kotlinlang.org](https://kotlinlang.org/) | Kotlin 2.0 | 扫描 `fun`, `val`, `var`。验证协程（Coroutines）和空安全（Null Safety）操作符。 |
| **.scala** | Scala | [scala-lang.org](https://www.scala-lang.org/) | Scala 3.4 | 扫描 `object`, `class`, `trait`。验证最新的泛型和隐式参数语法。 |
| **.pl/.pm** | Perl | [perl.org](https://www.perl.org/) | Perl 5.38+ | 扫描包（`package`）和子例程（`sub`）。验证正则表达式和上下文语法。 |
| **.lua** | Lua | [lua.org](https://www.lua.org/) | Lua 5.4 | 扫描函数（`function`）和表（`table`）。验证元表（Metatables）和协程（Coroutines）语法。 |
| **.dart** | Dart | [dart.dev](https://dart.dev/) | Dart 3.4 | 扫描类（`class`）和函数（`function`）。验证空安全和异步特性。 |
| **.jl** | Julia | [julialang.org](https://julialang.org/) | Julia 1.10+ | 扫描函数（`function`）和结构体（`struct`）。验证多重分派（Multiple Dispatch）语法。 |
| **.hs** | GHC | [haskell.org](https://www.haskell.org/) | Haskell 2010 / GHC 9.8 | 扫描模块（`module`）和类型签名（Type Signatures）。验证惰性求值和 Monad 语法。 |
| **.ex/.exs** | Elixir | [elixir-lang.org](https://elixir-lang.org/) | Elixir 1.16 | 扫描模块（`defmodule`）和函数（`def`）。验证管道操作符（`|>`）和模式匹配。 |
| **.sh/.bash** | POSIX Shell | [gnu.org/software/bash](https://www.gnu.org/software/bash/) | Bash 5.2 | 解析 Shebang（`#!/bin/bash`），验证函数定义、变量扩展和控制流语法。 |
| **.bat/.cmd** | Windows CMD | [learn.microsoft.com/en-us/windows-server/administration/](https://learn.microsoft.com/en-us/windows-server/administration/) | Windows Batch | 解析 `@echo off`，验证 `set`, `if`, `for` 循环和批处理标签。 |
| **.sql** | SQLite, PostgreSQL | [sqlite.org](https://www.sqlite.org/) / [postgresql.org](https://www.postgresql.org/) | SQL:2023 | 词法分析器。解析 `CREATE TABLE`, `SELECT`, `JOIN`。验证最新的窗口函数和 JSON 操作符。 |
| **.ts** | TypeScript (tsc) | [typescriptlang.org](https://www.typescriptlang.org/) | TypeScript 5.x | 词法分析器。验证接口（`interface`）、类型别名（`type`）和装饰器（Decorators）语法。 |
| **.php** | PHP Group | [php.net](https://www.php.net/) | PHP 8.4 | 扫描 `<?php` 标签。验证最新的 JIT 编译器提示、属性（Attributes）和枚举（Enums）语法。 |
| **.go** | Golang (gc) | [go.dev](https://go.dev/) | Go 1.23+ | 扫描 `package main`。验证泛型（Generics）的 `[T any]` 和并发（Goroutines/Channels）语法。 |
| **.rs** | Rust (rustc) | [rust-lang.org](https://www.rust-lang.org/) | Rust 1.8x | 扫描 `fn main()`。验证所有权（Ownership）、借用（Borrowing）和生命周期（Lifetimes）语法。 |
| **.erl** | Erlang/OTP | [erlang.org](https://www.erlang.org/) | Erlang/OTP 27 | 扫描模块（`-module`）和导出（`-export`）。验证模式匹配和递归函数语法。 |
| **.clj** | Clojure | [clojure.org](https://clojure.org/) | Clojure 1.12 | 解析 S-表达式（S-expressions）。验证括号匹配、原子（Atoms）和宏（Macros）语法。 |
| **.asm** | NASM / MASM | [nasm.us](https://www.nasm.us/) | x86-64 / ARM64 | 解析段（`section`）、指令（`mov`, `add`）和寄存器。验证汇编语法和调用约定。 |
| **.ps1** | PowerShell | [learn.microsoft.com/en-us/powershell](https://learn.microsoft.com/en-us/powershell/) | PowerShell 7.4 | 解析 Cmdlet（`Verb-Noun` 结构）。验证管道（Pipeline）和脚本块（ScriptBlock）语法。 |
| **.vba** | Microsoft VBA | [learn.microsoft.com/en-us/office/vba](https://learn.microsoft.com/en-us/office/vba/) | VBA 7.1 | 扫描 `Sub` 和 `Function`。验证对象模型引用（如 Excel/Word API）和错误处理（`On Error`）。 |
| **.r** | R Language | [r-project.org](https://www.r-project.org/) | R 4.4+ | 扫描函数定义（`function`）和赋值（`<-`）。验证向量化操作和数据框（Data Frame）语法。 |

### 七、 项目文件与工程文件 (Project & Engineering Files)

| 目标后缀 | 主流/官方核心工具 | 官方下载与规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.sb3** | Scratch VM / Parser | [scratch.mit.edu](https://scratch.mit.edu) / [github.com/LLK/scratch-parser](https://github.com/LLK/scratch-parser) | Scratch 3.x (HTML5/JS) | **本质是 ZIP**。定位并解压 `project.json`，解析 JSON 提取 `targets` (精灵/舞台) 和 `monitors` (变量监视器)。 |
| **.aep** | Adobe After Effects | [adobe.com/products/aftereffects](https://www.adobe.com/products/aftereffects) | AE 2026 | 解析专有二进制头部，提取项目元数据和合成（Composition）设置。 |
| **.prproj** | Adobe Premiere Pro | [adobe.com/products/premiere](https://www.adobe.com/products/premiere) | Premiere 2026 | 通常为 Gzip 压缩的 XML/SQLite。解压后解析时间线（Timeline）和序列（Sequence）数据。 |
| **.fcpxml** | Apple Final Cut Pro | [apple.com/final-cut-pro](https://www.apple.com/final-cut-pro/) | FCPXML 1.11+ | 纯 XML 格式。解析 `<library>`, `<event>`, `<project>` 标签提取剪辑时间线和资源引用。 |
| **.drp** | DaVinci Resolve | [blackmagicdesign.com/products/davinciresolve](https://www.blackmagicdesign.com/products/davinciresolve) | Resolve 19/20 | 专有 SQLite 数据库。读取数据库表结构，提取时间线和媒体池（Media Pool）信息。 |
| **.sln** | Visual Studio | [visualstudio.microsoft.com](https://visualstudio.microsoft.com/) | VS 2026 | 纯文本格式。解析 `Microsoft Visual Studio Solution File` 头部，提取项目 GUID 和配置映射。 |
| **.xcodeproj** | Xcode | [developer.apple.com/xcode](https://developer.apple.com/xcode/) | Xcode 16+ | 目录结构。核心是 `project.pbxproj`，解析类似 JSON 的 Plist 格式提取文件引用和构建设置。 |
| **.idea** | IntelliJ IDEA | [jetbrains.com/idea](https://www.jetbrains.com/idea/) | IntelliJ 2026 | 目录结构。解析 `.idea/` 下的 XML 文件（如 `modules.xml`, `workspace.xml`）提取项目模块配置。 |
| **.dockerfile** | Docker | [docker.com](https://www.docker.com/) | Dockerfile Reference | 纯文本指令集。解析 `FROM`, `RUN`, `CMD`, `COPY` 等指令，验证基础镜像和构建步骤。 |
| **.tf** | Terraform | [terraform.io](https://www.terraform.io/) | Terraform 1.9+ | HCL (HashiCorp Configuration Language)。解析 `resource`, `variable`, `provider` 块，验证基础设施即代码（IaC）语法。 |
| **.vagrant** | Vagrant | [vagrantup.com](https://www.vagrantup.com/) | Vagrant 2.4+ | Ruby DSL。解析 `Vagrant.configure` 块，提取虚拟机 Box、网络和同步文件夹配置。 |
| **.git** | Git SCM | [git-scm.com](https://git-scm.com/downloads) | SHA-256 过渡 | 读前几字节，若为 `78 9C` (zlib)，解压寻 `blob`/`tree`/`commit` 对象头。 |

### 八、 3D、游戏引擎与 CAD 类 (3D, Game Engines & CAD)

| 目标后缀 | 主流解析器 | 官方规范主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.blend** | Blender | [blender.org](https://www.blender.org/) | Blender 4.x 资产 | 解析 `BLENDER-v` 头部，读取 SDNA (Structure DNA) 索引表，验证节点树结构。 |
| **.ma/.mb** | Autodesk Maya | [autodesk.com/products/maya](https://www.autodesk.com/products/maya/) | Maya 2026 | `.ma` 为 ASCII，解析 `//` 注释和属性设置；`.mb` 为二进制，解析 IFF 块结构。 |
| **.max** | Autodesk 3ds Max | [autodesk.com/products/3ds-max](https://www.autodesk.com/products/3ds-max/) | 3ds Max 2026 | 解析 OLE2 复合文件，提取场景节点层次和修改器堆栈。 |
| **.c4d** | Maxon Cinema 4D | [maxon.net](https://www.maxon.net/) | Cinema 4D 2026 | 解析专有的块状结构，提取对象树和材质球参数。 |
| **.unity** | Unity Technologies | [unity.com](https://unity.com/) | Unity 6 | 解析 YAML 头部，提取场景中的 GameObject 组件（Transform, Renderer）引用。 |
| **.uasset** | Unreal Engine | [unrealengine.com](https://www.unrealengine.com/) | UE5 Nanite/Lumen | 解析 Unreal Package Header，提取导入/导出表（Import/Export Tables）验证 Nanite 网格数据引用。 |
| **.godot/.tscn** | Godot Engine | [godotengine.org](https://godotengine.org/) | Godot 4.3 | 解析基于文本的场景文件，提取 `[node]`, `[resource]` 和 `[ext_resource]` 块。 |
| **.stl** | 各切片软件 | [fabbers.com/tech/STL_Format](https://www.fabbers.com/tech/STL_Format) | 彩色 STL 扩展 | 判断前 80字节 Header。ASCII 寻 `solid`；Binary 读第 84字节后的 4字节 Triangle Count (小端序)。 |
| **.obj** | Wavefront | [paulbourke.net/dataformats/obj](http://paulbourke.net/dataformats/obj/) | 材质扩展 | 纯文本格式。按行读取，统计 `v`, `vn`, `f` 数量，验证最新 `.mtl` 引用路径。 |
| **.fbx** | Autodesk FBX | [autodesk.com/developer-network/](https://www.autodesk.com/developer-network/) | FBX 2026 | 解析 ASCII 或二进制格式。提取对象定义（`Definitions`）和连接关系（`Connections`）。 |
| **.gltf/.glb** | Khronos Group | [github.com/KhronosGroup/glTF](https://github.com/KhronosGroup/glTF) | **glTF 2.0 / 3.0** (PBR) | `.gltf` 提取 `meshes` 节点；`.glb` 读取 12字节 Header，定位 JSON/BIN Chunk，验证 PBR 贴图索引。 |

### 九、 系统、字体、安全与虚拟化类 (System, Fonts, Security & Virtualization)

| 目标后缀 | 主流解析器/工具 | 官方主页 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.msi** | Microsoft Installer | [learn.microsoft.com/en-us/windows/win32/](https://learn.microsoft.com/en-us/windows/win32/) | Windows Installer 5.0 | 解析 OLE2 复合文件，读取 `_Tables` 流提取安装数据库表。 |
| **.app** | macOS Bundle | [developer.apple.com](https://developer.apple.com/) | macOS Sequoia+ | 解析 `Info.plist`，提取 `CFBundleExecutable` 和 `CFBundleVersion`。 |
| **.pkg** | macOS / Linux | [apple.com/macos](https://www.apple.com/macos/) | Installer Package | 解析 xar 归档结构，读取 `Distribution` XML 脚本。 |
| **.ttf** | Microsoft/Apple | [learn.microsoft.com/en-us/typography/](https://learn.microsoft.com/en-us/typography/) | TrueType / OpenType | 解析 Offset Table，读取 `cmap`, `glyf`, `name` 表提取字形数据和版权信息。 |
| **.otf** | Adobe / Microsoft | [adobe.com/fonts](https://www.adobe.com/fonts/) | OpenType 1.9 | 与 TTF 类似，但使用 CFF (Compact Font Format) 轮廓数据。 |
| **.woff/.woff2** | W3C | [w3.org/TR/WOFF](https://www.w3.org/TR/WOFF/) | WOFF2 | 解析 WOFF Header，验证压缩算法（Brotli）和字体元数据块。 |
| **.pem/.crt/.key** | OpenSSL | [openssl.org](https://www.openssl.org/) | X.509 v3 证书 | 剥离 Base64 头，解析 ASN.1 `TLV` (Tag-Length-Value) 结构，提取最新椭圆曲线 OID。 |
| **.vmdk** | VMware | [vmware.com](https://www.vmware.com/) | Virtual Disk 3.0 | 解析稀疏扩展（Sparse Extent）头部，提取虚拟磁盘大小和几何参数。 |
| **.vdi** | VirtualBox | [virtualbox.org](https://www.virtualbox.org/) | Virtual Disk Image | 解析 VDI Header，验证 UUID 和块分配表（Block Allocation Table）。 |
| **.vhd/.vhdx** | Microsoft | [learn.microsoft.com/en-us/windows-server/](https://learn.microsoft.com/en-us/windows-server/) | VHDX | 验证 `conectix` 尾部块，解析动态磁盘的 BAT (Block Allocation Table)。 |
| **.rom/.nes** | 各模拟器 | [nesdev.org](https://www.nesdev.org/) | NES 2.0 | 验证 `NES\x1a` 幻数。读取 PRG ROM 和 CHR ROM 的大小，提取 Mapper 编号。 |
| **.sav** | 游戏模拟器 | [emulation.gametechwiki.com](https://emulation.gametechwiki.com/) | 各平台存档 | 解析特定平台的存档头部，提取玩家进度和校验和。 |
| **.torrent** | BitTorrent | [bittorrent.org/beps](https://www.bittorrent.org/beps/bep_0000.html) | BEP 扩展 | 编写 **Bencode 解码器**。提取 `info` 字典计算 SHA-1 InfoHash。 |
| **.dat/.bak** | 通用数据 | [en.wikipedia.org/wiki/Data_file](https://en.wikipedia.org/wiki/Data_file/) | 自定义二进制/文本 | 执行启发式扫描（Heuristic Scan），识别常见的文件头（Magic Numbers）或文本编码。 |

### 十、 特殊/补充/自定义格式 (Special / Custom / Legacy)

| 目标后缀 | 主流解析器/工具 | 官方主页/规范 | 最新规范支持 (2026) | 我们的最小化解析路径 (Minimal Path) |
| :--- | :--- | :--- | :--- | :--- |
| **.enx/.edg/.oc** | 自定义/特定应用 | [github.com/RemixWorld-cn/FilenameLongAndLong](https://github.com/RemixWorld-cn/FilenameLongAndLong) | 团队自定义协议 | 基于团队定义的私有协议规范，解析特定的头部标识和版本字段。 |
| **.ydf** | YAML 变体 | [yaml.org](https://yaml.org/) | YAML 1.2 | 基于 YAML 1.2 规范进行数据交换解析。 |
| **.f/.k/.g/.v/.x** | Fortran/自定义 | [fortran-lang.org](https://fortran-lang.org/) | Fortran 2023 / 自定义 | `.f` 解析 Fortran 固定格式代码；其他后缀基于团队定义的文本/二进制结构解析。 |
| **.tml** | XML/HTML 变体 | [w3.org/TR/xml](https://www.w3.org/TR/xml/) | XML 1.1 | 解析 XML 声明和根元素，验证标签闭合和属性转义。 |

---
